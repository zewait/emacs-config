;;; package --- summary
;;; Commentary:
;;; code:
(setq org-todo-keywords
	  '((sequence "TODO(t)" "DOING(i!)" "HANGUP(h!)" "|" "DONE(d!)" "CANCEL(c!)")))

(provide 'setup-org)

;;; setup-org ends here
